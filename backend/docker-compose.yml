# Local development only — not for production.

version: "3.9"

services:
  api:
    build:
      context: .
    container_name: documind-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
